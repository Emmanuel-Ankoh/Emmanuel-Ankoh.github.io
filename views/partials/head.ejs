<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Developer portfolio of Emmanuel Ankoh" />
  <link rel="icon" href="/favicon.ico" />
  <meta name="description" content="<%= (meta && meta.description) || 'Portfolio of ' + (site && site.name || '') %>">
  <meta property="og:type" content="website">
  <meta property="og:title" content="<%= title %> · <%= (site && site.name) || '' %>">
  <meta property="og:description" content="<%= (meta && meta.description) || '' %>">
  <meta property="og:url" content="<%= (site && site.baseUrl) ? site.baseUrl + (typeof urlPath !== 'undefined' ? urlPath : '') : '' %>">
  <meta property="og:image" content="<%= (meta && meta.image) || '/images/placeholder.svg' %>">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="<%= title %> · <%= (site && site.name) || '' %>">
  <meta name="twitter:description" content="<%= (meta && meta.description) || '' %>">
  <meta name="twitter:image" content="<%= (meta && meta.image) || '/images/placeholder.svg' %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
    <title><%= title %> · Emmanuel Ankoh</title>
  </head>
  <body>
  <a class="skip-link" href="#main">Skip to content</a>
  <%- include('nav') %>
