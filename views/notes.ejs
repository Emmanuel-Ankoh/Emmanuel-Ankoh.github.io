<%- include('partials/head', { title, meta }) %>
<main id="main" class="container">
  <section>
    <h1>Notes & Writing</h1>
    <p class="subtitle" style="margin-bottom:1.5rem">A stream of talks, articles, and quick thoughts worth sharing.</p>
  </section>
  <section class="notes-page">
    <% const notes = (profile && Array.isArray(profile.notes)) ? profile.notes.filter(n => n.title && (n.summary || n.url)) : []; %>
    <% if (notes.length) { %>
      <div class="notes-list">
        <% notes.forEach((entry, idx) => { %>
          <article class="note-card" id="note-<%= idx %>">
            <header>
              <h2><%= entry.title %></h2>
              <% if (entry.date) { %><p class="note-date"><%= entry.date %></p><% } %>
            </header>
            <% if (entry.summary) { %><p><%= entry.summary %></p><% } %>
            <div class="actions">
              <% if (entry.url) { %>
                <a class="btn btn-primary" href="<%= entry.url %>" target="_blank" rel="noopener">Read more</a>
              <% } %>
            </div>
          </article>
        <% }) %>
      </div>
    <% } else { %>
      <p>No notes yet. Add entries from Admin → Profile to populate this page.</p>
    <% } %>
  </section>
</main>
<%- include('partials/footer') %>
